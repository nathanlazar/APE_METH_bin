#!/bin/bash

# nathan dot lazar at gmail dot com

# Get the lengths of the chromosomes given the header in a .sam file

samtools view -S -H $1 | \
  awk 'BEGIN{OFS="\t"} {if( NR>1) {print $2, $3, NR}}' | \
  sed 's/SN://' | \
  sed 's/LN://' | \
  tail
